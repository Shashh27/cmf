[pytest]
# Pytest configuration for CMF-OMS backend

# Test discovery
testpaths = tests

# Output verbosity — shows each test name and PASSED/FAILED
addopts = -v --tb=short

# Show print() statements during tests (useful for debugging)
# Remove -s if you want cleaner output
# addopts = -v --tb=short -s