apiVersion: 1

providers:
  - name: CMF
    orgId: 1
    folder: CMF
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
